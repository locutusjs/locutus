// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const __locutus_source_fn = require('../../../../src/perl/core/pack.ts').pack
const __locutus_source_module_url = new URL("../../../../src/perl/core/pack.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "pack"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.pack = pack;\nconst toByte = (value) => ((Math.trunc(value) % 256) + 256) % 256;\nconst toChar = (value) => String.fromCharCode(toByte(value));\nfunction writeUInt16BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt16LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff);\n}\nfunction writeUInt32BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 24) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt32LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 24) & 0xff);\n}\nfunction nibble(char) {\n    const lower = char.toLowerCase();\n    if (lower >= '0' && lower <= '9') {\n        return lower.charCodeAt(0) - 48;\n    }\n    if (lower >= 'a' && lower <= 'f') {\n        return lower.charCodeAt(0) - 87;\n    }\n    return 0;\n}\nfunction packHex(hex, highFirst, nybbles) {\n    const normalized = hex.replace(/[^0-9a-fA-F]/g, '');\n    const take = nybbles === -1 ? normalized.length : Math.max(0, nybbles);\n    const input = normalized.slice(0, take);\n    let out = '';\n    for (let i = 0; i < input.length; i += 2) {\n        const a = nibble(input[i] ?? '0');\n        const b = nibble(input[i + 1] ?? '0');\n        const byte = highFirst ? ((a << 4) | b) : ((b << 4) | a);\n        out += toChar(byte);\n    }\n    if (input.length % 2 === 1) {\n        const last = nibble(input[input.length - 1] ?? '0');\n        out += toChar(highFirst ? (last << 4) : last);\n    }\n    return out;\n}\nfunction pack(template, ...values) {\n    //      discuss at: https://locutus.io/perl/pack/\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Implements a practical subset: A, a, C, n, N, v, V, H, h with optional counts and *.\n    //          note 2: Output is a binary JavaScript string (char codes 0-255).\n    //       example 1: pack('A5', 'hi')\n    //       returns 1: 'hi   '\n    //       example 2: pack('C*', 72, 105, 33)\n    //       returns 2: 'Hi!'\n    //       example 3: pack('n', 258).split('').map((ch) => ch.charCodeAt(0))\n    //       returns 3: [1, 2]\n    const format = String(template).replace(/\\s+/g, '');\n    let cursor = 0;\n    let argIndex = 0;\n    let out = '';\n    while (cursor < format.length) {\n        const directive = format[cursor];\n        if (!directive) {\n            break;\n        }\n        cursor += 1;\n        let countToken = '';\n        while (cursor < format.length && /[0-9*]/.test(format[cursor] ?? '')) {\n            countToken += format[cursor];\n            cursor += 1;\n        }\n        const count = countToken === '' ? 1 : countToken === '*' ? -1 : Number.parseInt(countToken, 10);\n        const nextString = () => String(values[argIndex++] ?? '');\n        const nextNumber = () => Number(values[argIndex++] ?? 0);\n        switch (directive) {\n            case 'A':\n            case 'a': {\n                const value = nextString();\n                const width = count === -1 ? value.length : Math.max(0, count);\n                const padded = directive === 'A' ? value.padEnd(width, ' ') : value.padEnd(width, '\\0');\n                out += padded.slice(0, width);\n                break;\n            }\n            case 'C': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += toChar(nextNumber());\n                }\n                break;\n            }\n            case 'n': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16BE(nextNumber());\n                }\n                break;\n            }\n            case 'v': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16LE(nextNumber());\n                }\n                break;\n            }\n            case 'N': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32BE(nextNumber());\n                }\n                break;\n            }\n            case 'V': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32LE(nextNumber());\n                }\n                break;\n            }\n            case 'H':\n            case 'h': {\n                const value = nextString();\n                out += packHex(value, directive === 'H', count);\n                break;\n            }\n            default:\n                break;\n        }\n    }\n    return out;\n}"
const __locutus_standalone_ts_code = "// perl/core/pack (target function module)\nconst toByte = (value) => ((Math.trunc(value) % 256) + 256) % 256;\nconst toChar = (value) => String.fromCharCode(toByte(value));\nfunction writeUInt16BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt16LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff);\n}\nfunction writeUInt32BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 24) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt32LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 24) & 0xff);\n}\nfunction nibble(char) {\n    const lower = char.toLowerCase();\n    if (lower >= '0' && lower <= '9') {\n        return lower.charCodeAt(0) - 48;\n    }\n    if (lower >= 'a' && lower <= 'f') {\n        return lower.charCodeAt(0) - 87;\n    }\n    return 0;\n}\nfunction packHex(hex, highFirst, nybbles) {\n    const normalized = hex.replace(/[^0-9a-fA-F]/g, '');\n    const take = nybbles === -1 ? normalized.length : Math.max(0, nybbles);\n    const input = normalized.slice(0, take);\n    let out = '';\n    for (let i = 0; i < input.length; i += 2) {\n        const a = nibble(input[i] ?? '0');\n        const b = nibble(input[i + 1] ?? '0');\n        const byte = highFirst ? ((a << 4) | b) : ((b << 4) | a);\n        out += toChar(byte);\n    }\n    if (input.length % 2 === 1) {\n        const last = nibble(input[input.length - 1] ?? '0');\n        out += toChar(highFirst ? (last << 4) : last);\n    }\n    return out;\n}\nfunction pack(template, ...values) {\n    //      discuss at: https://locutus.io/perl/pack/\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Implements a practical subset: A, a, C, n, N, v, V, H, h with optional counts and *.\n    //          note 2: Output is a binary JavaScript string (char codes 0-255).\n    //       example 1: pack('A5', 'hi')\n    //       returns 1: 'hi   '\n    //       example 2: pack('C*', 72, 105, 33)\n    //       returns 2: 'Hi!'\n    //       example 3: pack('n', 258).split('').map((ch) => ch.charCodeAt(0))\n    //       returns 3: [1, 2]\n    const format = String(template).replace(/\\s+/g, '');\n    let cursor = 0;\n    let argIndex = 0;\n    let out = '';\n    while (cursor < format.length) {\n        const directive = format[cursor];\n        if (!directive) {\n            break;\n        }\n        cursor += 1;\n        let countToken = '';\n        while (cursor < format.length && /[0-9*]/.test(format[cursor] ?? '')) {\n            countToken += format[cursor];\n            cursor += 1;\n        }\n        const count = countToken === '' ? 1 : countToken === '*' ? -1 : Number.parseInt(countToken, 10);\n        const nextString = () => String(values[argIndex++] ?? '');\n        const nextNumber = () => Number(values[argIndex++] ?? 0);\n        switch (directive) {\n            case 'A':\n            case 'a': {\n                const value = nextString();\n                const width = count === -1 ? value.length : Math.max(0, count);\n                const padded = directive === 'A' ? value.padEnd(width, ' ') : value.padEnd(width, '\\0');\n                out += padded.slice(0, width);\n                break;\n            }\n            case 'C': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += toChar(nextNumber());\n                }\n                break;\n            }\n            case 'n': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16BE(nextNumber());\n                }\n                break;\n            }\n            case 'v': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16LE(nextNumber());\n                }\n                break;\n            }\n            case 'N': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32BE(nextNumber());\n                }\n                break;\n            }\n            case 'V': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32LE(nextNumber());\n                }\n                break;\n            }\n            case 'H':\n            case 'h': {\n                const value = nextString();\n                out += packHex(value, directive === 'H', count);\n                break;\n            }\n            default:\n                break;\n        }\n    }\n    return out;\n}"
const __locutus_standalone_js_code = "// perl/core/pack (target function module)\nconst toByte = (value) => ((Math.trunc(value) % 256) + 256) % 256;\nconst toChar = (value) => String.fromCharCode(toByte(value));\nfunction writeUInt16BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt16LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff);\n}\nfunction writeUInt32BE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar((n >>> 24) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 8) & 0xff) + toChar(n & 0xff);\n}\nfunction writeUInt32LE(value) {\n    const n = Math.trunc(value) >>> 0;\n    return toChar(n & 0xff) + toChar((n >>> 8) & 0xff) + toChar((n >>> 16) & 0xff) + toChar((n >>> 24) & 0xff);\n}\nfunction nibble(char) {\n    const lower = char.toLowerCase();\n    if (lower >= '0' && lower <= '9') {\n        return lower.charCodeAt(0) - 48;\n    }\n    if (lower >= 'a' && lower <= 'f') {\n        return lower.charCodeAt(0) - 87;\n    }\n    return 0;\n}\nfunction packHex(hex, highFirst, nybbles) {\n    const normalized = hex.replace(/[^0-9a-fA-F]/g, '');\n    const take = nybbles === -1 ? normalized.length : Math.max(0, nybbles);\n    const input = normalized.slice(0, take);\n    let out = '';\n    for (let i = 0; i < input.length; i += 2) {\n        const a = nibble(input[i] ?? '0');\n        const b = nibble(input[i + 1] ?? '0');\n        const byte = highFirst ? (a << 4) | b : (b << 4) | a;\n        out += toChar(byte);\n    }\n    if (input.length % 2 === 1) {\n        const last = nibble(input[input.length - 1] ?? '0');\n        out += toChar(highFirst ? last << 4 : last);\n    }\n    return out;\n}\nfunction pack(template, ...values) {\n    //      discuss at: https://locutus.io/perl/pack/\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Implements a practical subset: A, a, C, n, N, v, V, H, h with optional counts and *.\n    //          note 2: Output is a binary JavaScript string (char codes 0-255).\n    //       example 1: pack('A5', 'hi')\n    //       returns 1: 'hi   '\n    //       example 2: pack('C*', 72, 105, 33)\n    //       returns 2: 'Hi!'\n    //       example 3: pack('n', 258).split('').map((ch) => ch.charCodeAt(0))\n    //       returns 3: [1, 2]\n    const format = String(template).replace(/\\s+/g, '');\n    let cursor = 0;\n    let argIndex = 0;\n    let out = '';\n    while (cursor < format.length) {\n        const directive = format[cursor];\n        if (!directive) {\n            break;\n        }\n        cursor += 1;\n        let countToken = '';\n        while (cursor < format.length && /[0-9*]/.test(format[cursor] ?? '')) {\n            countToken += format[cursor];\n            cursor += 1;\n        }\n        const count = countToken === '' ? 1 : countToken === '*' ? -1 : Number.parseInt(countToken, 10);\n        const nextString = () => String(values[argIndex++] ?? '');\n        const nextNumber = () => Number(values[argIndex++] ?? 0);\n        switch (directive) {\n            case 'A':\n            case 'a': {\n                const value = nextString();\n                const width = count === -1 ? value.length : Math.max(0, count);\n                const padded = directive === 'A' ? value.padEnd(width, ' ') : value.padEnd(width, '\\0');\n                out += padded.slice(0, width);\n                break;\n            }\n            case 'C': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += toChar(nextNumber());\n                }\n                break;\n            }\n            case 'n': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16BE(nextNumber());\n                }\n                break;\n            }\n            case 'v': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt16LE(nextNumber());\n                }\n                break;\n            }\n            case 'N': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32BE(nextNumber());\n                }\n                break;\n            }\n            case 'V': {\n                const repeat = count === -1 ? values.length - argIndex : Math.max(0, count);\n                for (let i = 0; i < repeat; i++) {\n                    out += writeUInt32LE(nextNumber());\n                }\n                break;\n            }\n            case 'H':\n            case 'h': {\n                const value = nextString();\n                out += packHex(value, directive === 'H', count);\n                break;\n            }\n            default:\n                break;\n        }\n    }\n    return out;\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as { [key: string]: unknown } }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/perl/core/pack.ts (tested in test/generated/perl/core/pack.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = 'hi   '
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (pack: typeof __locutus_source_fn) => {
      return pack('A5', 'hi')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 2', function () {
    const expected = 'Hi!'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (pack: typeof __locutus_source_fn) => {
      return pack('C*', 72, 105, 33)
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 3', function () {
    const expected = [1, 2]
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (pack: typeof __locutus_source_fn) => {
      return pack('n', 258).split('').map((ch) => ch.charCodeAt(0))
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
