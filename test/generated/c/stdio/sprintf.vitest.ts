// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const __locutus_source_fn = require('../../../../src/c/stdio/sprintf.ts').sprintf
const __locutus_source_module_url = new URL("../../../../src/c/stdio/sprintf.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "sprintf"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.sprintf = sprintf;\nfunction pad(str, minLength, padChar, leftJustify) {\n    const diff = minLength - str.length;\n    const padStr = padChar.repeat(Math.max(0, diff));\n    return leftJustify ? str + padStr : padStr + str;\n}\nfunction sprintf(format, ...args) {\n    // original by: Rafał Kukawski\n    // bugfixed by: Param Siddharth\n    //   example 1: sprintf('%+10.*d', 5, 1)\n    //   returns 1: '    +00001'\n    //   example 2: sprintf('%s is a %d%% %s %s.', 'Param', 90, 'good', 'boy')\n    //   returns 2: 'Param is a 90% good boy.'\n    const placeholderRegex = /%(?:(\\d+)\\$)?([-+#0 ]*)(\\*|\\d+)?(?:\\.(\\*|\\d*))?([\\s\\S])/g;\n    let index = 0;\n    return format.replace(placeholderRegex, function (_match, param, flags, width, prec, modifier) {\n        const leftJustify = flags.includes('-');\n        // flag '0' is ignored when flag '-' is present\n        const padChar = leftJustify\n            ? ' '\n            : flags.split('').reduce((pc, c) => ([' ', '0'].includes(c) ? c : pc), ' ');\n        const positiveSign = flags.includes('+') ? '+' : flags.includes(' ') ? ' ' : '';\n        const minWidth = width === '*' ? Number(args[index++] ?? 0) : +(width ?? 0) || 0;\n        let precision = prec === '*' ? Number(args[index++] ?? 0) : prec === undefined ? Number.NaN : +prec;\n        const paramIndex = param ? +param : 0;\n        if (param && !paramIndex) {\n            throw new Error('Param index must be greater than 0');\n        }\n        if (param && paramIndex > args.length) {\n            throw new Error('Too few arguments');\n        }\n        // compiling with default clang params, mixed positional and non-positional params\n        // give only a warning\n        const arg = param ? args[paramIndex - 1] : args[index];\n        if (modifier !== '%') {\n            index++;\n        }\n        if (precision === undefined || isNaN(precision)) {\n            precision = 'eEfFgG'.includes(modifier) ? 6 : modifier === 's' ? String(arg).length : undefined;\n        }\n        switch (modifier) {\n            case '%':\n                return '%';\n            case 'd':\n            case 'i': {\n                const number = Math.trunc(Number(arg) || 0);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const str = pad(abs.toString(), precision || 0, '0', false);\n                if (padChar === '0') {\n                    return prefix + pad(str, minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(prefix + str, minWidth, padChar, leftJustify);\n            }\n            case 'e':\n            case 'E':\n            case 'f':\n            case 'F':\n            case 'g':\n            case 'G': {\n                const number = Number(arg);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const operationIndex = 'efg'.indexOf(modifier.toLowerCase());\n                const operation = [\n                    (value, fractionDigits) => value.toExponential(fractionDigits),\n                    (value, digits) => value.toFixed(digits),\n                    (value, precisionDigits) => value.toPrecision(precisionDigits),\n                ][operationIndex];\n                const transform = [(value) => value.toLowerCase(), (value) => value.toUpperCase()]['eEfFgG'.indexOf(modifier) % 2] ?? ((value) => value);\n                const isSpecial = isNaN(abs) || !isFinite(abs);\n                const str = isSpecial\n                    ? abs.toString().substr(0, 3)\n                    : (operation || ((value) => value.toString()))(abs, precision);\n                if (padChar === '0' && !isSpecial) {\n                    return prefix + pad(transform(str), minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(transform(prefix + str), minWidth, isSpecial ? ' ' : padChar, leftJustify);\n            }\n            case 'b':\n            case 'o':\n            case 'u':\n            case 'x':\n            case 'X': {\n                const number = Number(arg) || 0;\n                const intVal = Math.trunc(number) + (number < 0 ? 0xffffffff + 1 : 0);\n                const base = [2, 8, 10, 16, 16]['bouxX'.indexOf(modifier)] ?? 10;\n                const prefix = intVal && flags.includes('#') ? ['', '0', '', '0x', '0X']['bouxXX'.indexOf(modifier)] : '';\n                if (padChar === '0' && prefix) {\n                    return (prefix +\n                        pad(pad(intVal.toString(base), precision ?? 0, '0', false), minWidth - prefix.length, padChar, leftJustify));\n                }\n                return pad(prefix + pad(intVal.toString(base), precision ?? 0, '0', false), minWidth, padChar, leftJustify);\n            }\n            case 'p':\n            case 'n': {\n                throw new Error(`'${modifier}' modifier not supported`);\n            }\n            case 's': {\n                return pad(String(arg).substr(0, precision), minWidth, padChar, leftJustify);\n            }\n            case 'c': {\n                // extension, if arg is string, take first char\n                const chr = typeof arg === 'string' ? arg.charAt(0) : String.fromCharCode(Number(arg));\n                return pad(chr, minWidth, padChar, leftJustify);\n            }\n            case 'a':\n            case 'A':\n                throw new Error(`'${modifier}' modifier not yet implemented`);\n            default:\n                // for unknown modifiers, return the modifier char\n                return modifier;\n        }\n    });\n}"
const __locutus_standalone_ts_code = "// c/stdio/sprintf\nfunction pad(str, minLength, padChar, leftJustify) {\n    const diff = minLength - str.length;\n    const padStr = padChar.repeat(Math.max(0, diff));\n    return leftJustify ? str + padStr : padStr + str;\n}\nfunction sprintf(format, ...args) {\n    // original by: Rafał Kukawski\n    // bugfixed by: Param Siddharth\n    //   example 1: sprintf('%+10.*d', 5, 1)\n    //   returns 1: '    +00001'\n    //   example 2: sprintf('%s is a %d%% %s %s.', 'Param', 90, 'good', 'boy')\n    //   returns 2: 'Param is a 90% good boy.'\n    const placeholderRegex = /%(?:(\\d+)\\$)?([-+#0 ]*)(\\*|\\d+)?(?:\\.(\\*|\\d*))?([\\s\\S])/g;\n    let index = 0;\n    return format.replace(placeholderRegex, function (_match, param, flags, width, prec, modifier) {\n        const leftJustify = flags.includes('-');\n        // flag '0' is ignored when flag '-' is present\n        const padChar = leftJustify\n            ? ' '\n            : flags.split('').reduce((pc, c) => ([' ', '0'].includes(c) ? c : pc), ' ');\n        const positiveSign = flags.includes('+') ? '+' : flags.includes(' ') ? ' ' : '';\n        const minWidth = width === '*' ? Number(args[index++] ?? 0) : +(width ?? 0) || 0;\n        let precision = prec === '*' ? Number(args[index++] ?? 0) : prec === undefined ? Number.NaN : +prec;\n        const paramIndex = param ? +param : 0;\n        if (param && !paramIndex) {\n            throw new Error('Param index must be greater than 0');\n        }\n        if (param && paramIndex > args.length) {\n            throw new Error('Too few arguments');\n        }\n        // compiling with default clang params, mixed positional and non-positional params\n        // give only a warning\n        const arg = param ? args[paramIndex - 1] : args[index];\n        if (modifier !== '%') {\n            index++;\n        }\n        if (precision === undefined || isNaN(precision)) {\n            precision = 'eEfFgG'.includes(modifier) ? 6 : modifier === 's' ? String(arg).length : undefined;\n        }\n        switch (modifier) {\n            case '%':\n                return '%';\n            case 'd':\n            case 'i': {\n                const number = Math.trunc(Number(arg) || 0);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const str = pad(abs.toString(), precision || 0, '0', false);\n                if (padChar === '0') {\n                    return prefix + pad(str, minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(prefix + str, minWidth, padChar, leftJustify);\n            }\n            case 'e':\n            case 'E':\n            case 'f':\n            case 'F':\n            case 'g':\n            case 'G': {\n                const number = Number(arg);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const operationIndex = 'efg'.indexOf(modifier.toLowerCase());\n                const operation = [\n                    (value, fractionDigits) => value.toExponential(fractionDigits),\n                    (value, digits) => value.toFixed(digits),\n                    (value, precisionDigits) => value.toPrecision(precisionDigits),\n                ][operationIndex];\n                const transform = [(value) => value.toLowerCase(), (value) => value.toUpperCase()]['eEfFgG'.indexOf(modifier) % 2] ?? ((value) => value);\n                const isSpecial = isNaN(abs) || !isFinite(abs);\n                const str = isSpecial\n                    ? abs.toString().substr(0, 3)\n                    : (operation || ((value) => value.toString()))(abs, precision);\n                if (padChar === '0' && !isSpecial) {\n                    return prefix + pad(transform(str), minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(transform(prefix + str), minWidth, isSpecial ? ' ' : padChar, leftJustify);\n            }\n            case 'b':\n            case 'o':\n            case 'u':\n            case 'x':\n            case 'X': {\n                const number = Number(arg) || 0;\n                const intVal = Math.trunc(number) + (number < 0 ? 0xffffffff + 1 : 0);\n                const base = [2, 8, 10, 16, 16]['bouxX'.indexOf(modifier)] ?? 10;\n                const prefix = intVal && flags.includes('#') ? ['', '0', '', '0x', '0X']['bouxXX'.indexOf(modifier)] : '';\n                if (padChar === '0' && prefix) {\n                    return (prefix +\n                        pad(pad(intVal.toString(base), precision ?? 0, '0', false), minWidth - prefix.length, padChar, leftJustify));\n                }\n                return pad(prefix + pad(intVal.toString(base), precision ?? 0, '0', false), minWidth, padChar, leftJustify);\n            }\n            case 'p':\n            case 'n': {\n                throw new Error(`'${modifier}' modifier not supported`);\n            }\n            case 's': {\n                return pad(String(arg).substr(0, precision), minWidth, padChar, leftJustify);\n            }\n            case 'c': {\n                // extension, if arg is string, take first char\n                const chr = typeof arg === 'string' ? arg.charAt(0) : String.fromCharCode(Number(arg));\n                return pad(chr, minWidth, padChar, leftJustify);\n            }\n            case 'a':\n            case 'A':\n                throw new Error(`'${modifier}' modifier not yet implemented`);\n            default:\n                // for unknown modifiers, return the modifier char\n                return modifier;\n        }\n    });\n}"
const __locutus_standalone_js_code = "// c/stdio/sprintf\nfunction pad(str, minLength, padChar, leftJustify) {\n    const diff = minLength - str.length;\n    const padStr = padChar.repeat(Math.max(0, diff));\n    return leftJustify ? str + padStr : padStr + str;\n}\nfunction sprintf(format, ...args) {\n    // original by: Rafał Kukawski\n    // bugfixed by: Param Siddharth\n    //   example 1: sprintf('%+10.*d', 5, 1)\n    //   returns 1: '    +00001'\n    //   example 2: sprintf('%s is a %d%% %s %s.', 'Param', 90, 'good', 'boy')\n    //   returns 2: 'Param is a 90% good boy.'\n    const placeholderRegex = /%(?:(\\d+)\\$)?([-+#0 ]*)(\\*|\\d+)?(?:\\.(\\*|\\d*))?([\\s\\S])/g;\n    let index = 0;\n    return format.replace(placeholderRegex, function (_match, param, flags, width, prec, modifier) {\n        const leftJustify = flags.includes('-');\n        // flag '0' is ignored when flag '-' is present\n        const padChar = leftJustify\n            ? ' '\n            : flags.split('').reduce((pc, c) => ([' ', '0'].includes(c) ? c : pc), ' ');\n        const positiveSign = flags.includes('+') ? '+' : flags.includes(' ') ? ' ' : '';\n        const minWidth = width === '*' ? Number(args[index++] ?? 0) : +(width ?? 0) || 0;\n        let precision = prec === '*' ? Number(args[index++] ?? 0) : prec === undefined ? Number.NaN : +prec;\n        const paramIndex = param ? +param : 0;\n        if (param && !paramIndex) {\n            throw new Error('Param index must be greater than 0');\n        }\n        if (param && paramIndex > args.length) {\n            throw new Error('Too few arguments');\n        }\n        // compiling with default clang params, mixed positional and non-positional params\n        // give only a warning\n        const arg = param ? args[paramIndex - 1] : args[index];\n        if (modifier !== '%') {\n            index++;\n        }\n        if (precision === undefined || isNaN(precision)) {\n            precision = 'eEfFgG'.includes(modifier) ? 6 : modifier === 's' ? String(arg).length : undefined;\n        }\n        switch (modifier) {\n            case '%':\n                return '%';\n            case 'd':\n            case 'i': {\n                const number = Math.trunc(Number(arg) || 0);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const str = pad(abs.toString(), precision || 0, '0', false);\n                if (padChar === '0') {\n                    return prefix + pad(str, minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(prefix + str, minWidth, padChar, leftJustify);\n            }\n            case 'e':\n            case 'E':\n            case 'f':\n            case 'F':\n            case 'g':\n            case 'G': {\n                const number = Number(arg);\n                const abs = Math.abs(number);\n                const prefix = number < 0 ? '-' : positiveSign;\n                const operationIndex = 'efg'.indexOf(modifier.toLowerCase());\n                const operation = [\n                    (value, fractionDigits) => value.toExponential(fractionDigits),\n                    (value, digits) => value.toFixed(digits),\n                    (value, precisionDigits) => value.toPrecision(precisionDigits),\n                ][operationIndex];\n                const transform = [(value) => value.toLowerCase(), (value) => value.toUpperCase()]['eEfFgG'.indexOf(modifier) % 2] ?? ((value) => value);\n                const isSpecial = isNaN(abs) || !isFinite(abs);\n                const str = isSpecial\n                    ? abs.toString().substr(0, 3)\n                    : (operation || ((value) => value.toString()))(abs, precision);\n                if (padChar === '0' && !isSpecial) {\n                    return prefix + pad(transform(str), minWidth - prefix.length, padChar, leftJustify);\n                }\n                return pad(transform(prefix + str), minWidth, isSpecial ? ' ' : padChar, leftJustify);\n            }\n            case 'b':\n            case 'o':\n            case 'u':\n            case 'x':\n            case 'X': {\n                const number = Number(arg) || 0;\n                const intVal = Math.trunc(number) + (number < 0 ? 0xffffffff + 1 : 0);\n                const base = [2, 8, 10, 16, 16]['bouxX'.indexOf(modifier)] ?? 10;\n                const prefix = intVal && flags.includes('#') ? ['', '0', '', '0x', '0X']['bouxXX'.indexOf(modifier)] : '';\n                if (padChar === '0' && prefix) {\n                    return (prefix +\n                        pad(pad(intVal.toString(base), precision ?? 0, '0', false), minWidth - prefix.length, padChar, leftJustify));\n                }\n                return pad(prefix + pad(intVal.toString(base), precision ?? 0, '0', false), minWidth, padChar, leftJustify);\n            }\n            case 'p':\n            case 'n': {\n                throw new Error(`'${modifier}' modifier not supported`);\n            }\n            case 's': {\n                return pad(String(arg).substr(0, precision), minWidth, padChar, leftJustify);\n            }\n            case 'c': {\n                // extension, if arg is string, take first char\n                const chr = typeof arg === 'string' ? arg.charAt(0) : String.fromCharCode(Number(arg));\n                return pad(chr, minWidth, padChar, leftJustify);\n            }\n            case 'a':\n            case 'A':\n                throw new Error(`'${modifier}' modifier not yet implemented`);\n            default:\n                // for unknown modifiers, return the modifier char\n                return modifier;\n        }\n    });\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as Record<string, unknown> }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/c/stdio/sprintf.ts (tested in test/generated/c/stdio/sprintf.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = '    +00001'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sprintf: typeof __locutus_source_fn) => {
      return sprintf('%+10.*d', 5, 1)
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 2', function () {
    const expected = 'Param is a 90% good boy.'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sprintf: typeof __locutus_source_fn) => {
      return sprintf('%s is a %d%% %s %s.', 'Param', 90, 'good', 'boy')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
