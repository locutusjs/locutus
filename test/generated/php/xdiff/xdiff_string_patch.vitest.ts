// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const ini_set = require('../../../../src/php/info/ini_set.ts').ini_set
const ini_get = require('../../../../src/php/info/ini_get.ts').ini_get
const __locutus_source_fn = require('../../../../src/php/xdiff/xdiff_string_patch.ts').xdiff_string_patch
const __locutus_source_module_url = new URL("../../../../src/php/xdiff/xdiff_string_patch.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "xdiff_string_patch"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.xdiff_string_patch = xdiff_string_patch;\nconst _phpRuntimeState_ts_1 = require(\"../_helpers/_phpRuntimeState.ts\");\nfunction xdiff_string_patch(originalStr, patch, flags, errorObj) {\n    //      discuss at: https://locutus.io/php/xdiff_string_patch/\n    // parity verified: PHP 8.3\n    //     original by: Brett Zamir (https://brett-zamir.me)\n    //     improved by: Steven Levithan (stevenlevithan.com)\n    //          note 1: The XDIFF_PATCH_IGNORESPACE flag and the error argument are not\n    //          note 1: currently supported.\n    //          note 2: This has not been tested exhaustively yet.\n    //          note 3: The errorObj parameter (optional) if used must be passed in as a\n    //          note 3: object. The errors will then be written by reference into it's `value` property\n    //       example 1: xdiff_string_patch('', '@@ -0,0 +1,1 @@\\n+Hello world!')\n    //       returns 1: 'Hello world!'\n    // First two functions were adapted from Steven Levithan, also under an MIT license\n    // Adapted from XRegExp 1.5.0\n    // (c) 2007-2010 Steven Levithan\n    // MIT License\n    // <https://xregexp.com>\n    const isRecord = (value) => typeof value === 'object' && value !== null && !Array.isArray(value);\n    const _getNativeFlags = function (regex) {\n        const extended = (0, _phpRuntimeState_ts_1.getPhpObjectEntry)(regex, 'extended') === true;\n        const sticky = (0, _phpRuntimeState_ts_1.getPhpObjectEntry)(regex, 'sticky') === true;\n        // Proposed for ES4; included in AS3\n        return [\n            regex.global ? 'g' : '',\n            regex.ignoreCase ? 'i' : '',\n            regex.multiline ? 'm' : '',\n            extended ? 'x' : '',\n            sticky ? 'y' : '',\n        ].join('');\n    };\n    const getXRegExpMeta = (regex) => {\n        const xregexpValue = (0, _phpRuntimeState_ts_1.getPhpObjectEntry)(regex, '_xregexp');\n        if (!isRecord(xregexpValue)) {\n            return undefined;\n        }\n        const sourceValue = (0, _phpRuntimeState_ts_1.getPhpObjectEntry)(xregexpValue, 'source');\n        if (typeof sourceValue !== 'string') {\n            return undefined;\n        }\n        const captureNamesValue = (0, _phpRuntimeState_ts_1.getPhpObjectEntry)(xregexpValue, 'captureNames');\n        let captureNames;\n        if (captureNamesValue === null) {\n            captureNames = null;\n        }\n        else if (Array.isArray(captureNamesValue) &&\n            captureNamesValue.every((captureName) => typeof captureName === 'string')) {\n            captureNames = captureNamesValue;\n        }\n        if (captureNames === undefined) {\n            return { source: sourceValue };\n        }\n        return {\n            source: sourceValue,\n            captureNames,\n        };\n    };\n    const _cbSplit = function (input, sep) {\n        // If separator `s` is not a regex, use the native `split`\n        if (!(sep instanceof RegExp)) {\n            return input.split(sep);\n        }\n        const str = String(input);\n        const output = [];\n        let lastLastIndex = 0;\n        let match = null;\n        let lastLength = 0;\n        const limit = Infinity;\n        const x = getXRegExpMeta(sep);\n        // This is required if not `s.global`, and it avoids needing to set `s.lastIndex` to zero\n        // and restore it to its original value when we're done using the regex\n        // Brett paring down\n        const s = new RegExp(sep.source, _getNativeFlags(sep) + 'g');\n        if (x) {\n            (0, _phpRuntimeState_ts_1.setPhpObjectEntry)(s, '_xregexp', {\n                source: x.source,\n                captureNames: x.captureNames ? x.captureNames.slice(0) : null,\n            });\n        }\n        while ((match = s.exec(str))) {\n            // Run the altered `exec` (required for `lastIndex` fix, etc.)\n            if (s.lastIndex > lastLastIndex) {\n                output.push(str.slice(lastLastIndex, match.index));\n                if (match.length > 1 && match.index < str.length) {\n                    const captures = match.slice(1).filter((capture) => capture !== undefined);\n                    Array.prototype.push.apply(output, captures);\n                }\n                lastLength = match[0].length;\n                lastLastIndex = s.lastIndex;\n                if (output.length >= limit) {\n                    break;\n                }\n            }\n            if (s.lastIndex === match.index) {\n                s.lastIndex++;\n            }\n        }\n        if (lastLastIndex === str.length) {\n            if (!s.test('') || lastLength) {\n                output.push('');\n            }\n        }\n        else {\n            output.push(str.slice(lastLastIndex));\n        }\n        return output.length > limit ? output.slice(0, limit) : output;\n    };\n    // Input defaulting & sanitation\n    if (!patch) {\n        return false;\n    }\n    let i = 0;\n    let ll = 0;\n    let ranges = null;\n    let lastLinePos = 0;\n    let firstChar = '';\n    const rangeExp = /^@@\\s+-(\\d+),(\\d+)\\s+\\+(\\d+),(\\d+)\\s+@@$/;\n    const lineBreaks = /\\r?\\n/;\n    const lines = _cbSplit(patch.replace(/(\\r?\\n)+$/, ''), lineBreaks);\n    const origLines = _cbSplit(originalStr, lineBreaks);\n    const newStrArr = [];\n    let linePos = 0;\n    const errors = '';\n    let optTemp = 0; // Both string & integer (constant) input is allowed\n    const OPTS = {\n        // Unsure of actual PHP values, so better to rely on string\n        XDIFF_PATCH_NORMAL: 1,\n        XDIFF_PATCH_REVERSE: 2,\n        XDIFF_PATCH_IGNORESPACE: 4,\n    };\n    if (!flags) {\n        flags = 'XDIFF_PATCH_NORMAL';\n    }\n    if (typeof flags !== 'number') {\n        // Allow for a single string or an array of string flags\n        const flagList = Array.isArray(flags) ? flags : [flags];\n        const isPatchOption = (value) => value in OPTS;\n        for (i = 0; i < flagList.length; i++) {\n            const currentFlag = flagList[i];\n            if (!currentFlag) {\n                continue;\n            }\n            // Resolve string input to bitwise e.g. 'XDIFF_PATCH_NORMAL' becomes 1\n            if (isPatchOption(currentFlag)) {\n                optTemp = optTemp | OPTS[currentFlag];\n            }\n        }\n        flags = optTemp;\n    }\n    if (flags & OPTS.XDIFF_PATCH_NORMAL) {\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[1]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[1]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case '+':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    else if (flags & OPTS.XDIFF_PATCH_REVERSE) {\n        // Only differs from above by a few lines\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[3]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[3]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case '+':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    if (errorObj) {\n        errorObj.value = errors;\n    }\n    return newStrArr.join('\\n');\n}"
const __locutus_standalone_ts_code = "function getPhpObjectEntry(value, key) {\n    if ((typeof value !== 'object' && typeof value !== 'function') || value === null) {\n        return undefined;\n    }\n    let current = value;\n    while (current) {\n        const descriptor = Object.getOwnPropertyDescriptor(current, key);\n        if (descriptor) {\n            if (typeof descriptor.get === 'function') {\n                const getterValue = descriptor.get.call(value);\n                return typeof getterValue === 'undefined' ? undefined : getterValue;\n            }\n            const directValue = descriptor.value;\n            return typeof directValue === 'undefined' ? undefined : directValue;\n        }\n        current = Object.getPrototypeOf(current);\n    }\n    return undefined;\n}\nfunction setPhpObjectEntry(value, key, entry) {\n    if ((typeof value !== 'object' && typeof value !== 'function') || value === null) {\n        return false;\n    }\n    const container = value;\n    let current = container;\n    while (current) {\n        const descriptor = Object.getOwnPropertyDescriptor(current, key);\n        if (descriptor) {\n            if (typeof descriptor.set === 'function') {\n                descriptor.set.call(container, entry);\n                return true;\n            }\n            if (descriptor.writable) {\n                Object.defineProperty(container, key, {\n                    configurable: descriptor.configurable ?? true,\n                    enumerable: descriptor.enumerable ?? true,\n                    value: entry,\n                    writable: true,\n                });\n                return true;\n            }\n            return false;\n        }\n        current = Object.getPrototypeOf(current);\n    }\n    Object.defineProperty(container, key, {\n        configurable: true,\n        enumerable: true,\n        value: entry,\n        writable: true,\n    });\n    return true;\n}\nfunction xdiff_string_patch(originalStr, patch, flags, errorObj) {\n    //      discuss at: https://locutus.io/php/xdiff_string_patch/\n    // parity verified: PHP 8.3\n    //     original by: Brett Zamir (https://brett-zamir.me)\n    //     improved by: Steven Levithan (stevenlevithan.com)\n    //          note 1: The XDIFF_PATCH_IGNORESPACE flag and the error argument are not\n    //          note 1: currently supported.\n    //          note 2: This has not been tested exhaustively yet.\n    //          note 3: The errorObj parameter (optional) if used must be passed in as a\n    //          note 3: object. The errors will then be written by reference into it's `value` property\n    //       example 1: xdiff_string_patch('', '@@ -0,0 +1,1 @@\\n+Hello world!')\n    //       returns 1: 'Hello world!'\n    // First two functions were adapted from Steven Levithan, also under an MIT license\n    // Adapted from XRegExp 1.5.0\n    // (c) 2007-2010 Steven Levithan\n    // MIT License\n    // <https://xregexp.com>\n    const isRecord = (value) => typeof value === 'object' && value !== null && !Array.isArray(value);\n    const _getNativeFlags = function (regex) {\n        const extended = getPhpObjectEntry(regex, 'extended') === true;\n        const sticky = getPhpObjectEntry(regex, 'sticky') === true;\n        // Proposed for ES4; included in AS3\n        return [\n            regex.global ? 'g' : '',\n            regex.ignoreCase ? 'i' : '',\n            regex.multiline ? 'm' : '',\n            extended ? 'x' : '',\n            sticky ? 'y' : '',\n        ].join('');\n    };\n    const getXRegExpMeta = (regex) => {\n        const xregexpValue = getPhpObjectEntry(regex, '_xregexp');\n        if (!isRecord(xregexpValue)) {\n            return undefined;\n        }\n        const sourceValue = getPhpObjectEntry(xregexpValue, 'source');\n        if (typeof sourceValue !== 'string') {\n            return undefined;\n        }\n        const captureNamesValue = getPhpObjectEntry(xregexpValue, 'captureNames');\n        let captureNames;\n        if (captureNamesValue === null) {\n            captureNames = null;\n        }\n        else if (Array.isArray(captureNamesValue) &&\n            captureNamesValue.every((captureName) => typeof captureName === 'string')) {\n            captureNames = captureNamesValue;\n        }\n        if (captureNames === undefined) {\n            return { source: sourceValue };\n        }\n        return {\n            source: sourceValue,\n            captureNames,\n        };\n    };\n    const _cbSplit = function (input, sep) {\n        // If separator `s` is not a regex, use the native `split`\n        if (!(sep instanceof RegExp)) {\n            return input.split(sep);\n        }\n        const str = String(input);\n        const output = [];\n        let lastLastIndex = 0;\n        let match = null;\n        let lastLength = 0;\n        const limit = Infinity;\n        const x = getXRegExpMeta(sep);\n        // This is required if not `s.global`, and it avoids needing to set `s.lastIndex` to zero\n        // and restore it to its original value when we're done using the regex\n        // Brett paring down\n        const s = new RegExp(sep.source, _getNativeFlags(sep) + 'g');\n        if (x) {\n            setPhpObjectEntry(s, '_xregexp', {\n                source: x.source,\n                captureNames: x.captureNames ? x.captureNames.slice(0) : null,\n            });\n        }\n        while ((match = s.exec(str))) {\n            // Run the altered `exec` (required for `lastIndex` fix, etc.)\n            if (s.lastIndex > lastLastIndex) {\n                output.push(str.slice(lastLastIndex, match.index));\n                if (match.length > 1 && match.index < str.length) {\n                    const captures = match.slice(1).filter((capture) => capture !== undefined);\n                    Array.prototype.push.apply(output, captures);\n                }\n                lastLength = match[0].length;\n                lastLastIndex = s.lastIndex;\n                if (output.length >= limit) {\n                    break;\n                }\n            }\n            if (s.lastIndex === match.index) {\n                s.lastIndex++;\n            }\n        }\n        if (lastLastIndex === str.length) {\n            if (!s.test('') || lastLength) {\n                output.push('');\n            }\n        }\n        else {\n            output.push(str.slice(lastLastIndex));\n        }\n        return output.length > limit ? output.slice(0, limit) : output;\n    };\n    // Input defaulting & sanitation\n    if (!patch) {\n        return false;\n    }\n    let i = 0;\n    let ll = 0;\n    let ranges = null;\n    let lastLinePos = 0;\n    let firstChar = '';\n    const rangeExp = /^@@\\s+-(\\d+),(\\d+)\\s+\\+(\\d+),(\\d+)\\s+@@$/;\n    const lineBreaks = /\\r?\\n/;\n    const lines = _cbSplit(patch.replace(/(\\r?\\n)+$/, ''), lineBreaks);\n    const origLines = _cbSplit(originalStr, lineBreaks);\n    const newStrArr = [];\n    let linePos = 0;\n    const errors = '';\n    let optTemp = 0; // Both string & integer (constant) input is allowed\n    const OPTS = {\n        // Unsure of actual PHP values, so better to rely on string\n        XDIFF_PATCH_NORMAL: 1,\n        XDIFF_PATCH_REVERSE: 2,\n        XDIFF_PATCH_IGNORESPACE: 4,\n    };\n    if (!flags) {\n        flags = 'XDIFF_PATCH_NORMAL';\n    }\n    if (typeof flags !== 'number') {\n        // Allow for a single string or an array of string flags\n        const flagList = Array.isArray(flags) ? flags : [flags];\n        const isPatchOption = (value) => value in OPTS;\n        for (i = 0; i < flagList.length; i++) {\n            const currentFlag = flagList[i];\n            if (!currentFlag) {\n                continue;\n            }\n            // Resolve string input to bitwise e.g. 'XDIFF_PATCH_NORMAL' becomes 1\n            if (isPatchOption(currentFlag)) {\n                optTemp = optTemp | OPTS[currentFlag];\n            }\n        }\n        flags = optTemp;\n    }\n    if (flags & OPTS.XDIFF_PATCH_NORMAL) {\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[1]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[1]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case '+':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    else if (flags & OPTS.XDIFF_PATCH_REVERSE) {\n        // Only differs from above by a few lines\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[3]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[3]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case '+':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    if (errorObj) {\n        errorObj.value = errors;\n    }\n    return newStrArr.join('\\n');\n}"
const __locutus_standalone_js_code = "// php/_helpers/_phpRuntimeState\nfunction getPhpObjectEntry(value, key) {\n    if ((typeof value !== 'object' && typeof value !== 'function') || value === null) {\n        return undefined;\n    }\n    let current = value;\n    while (current) {\n        const descriptor = Object.getOwnPropertyDescriptor(current, key);\n        if (descriptor) {\n            if (typeof descriptor.get === 'function') {\n                const getterValue = descriptor.get.call(value);\n                return typeof getterValue === 'undefined' ? undefined : getterValue;\n            }\n            const directValue = descriptor.value;\n            return typeof directValue === 'undefined' ? undefined : directValue;\n        }\n        current = Object.getPrototypeOf(current);\n    }\n    return undefined;\n}\nfunction setPhpObjectEntry(value, key, entry) {\n    if ((typeof value !== 'object' && typeof value !== 'function') || value === null) {\n        return false;\n    }\n    const container = value;\n    let current = container;\n    while (current) {\n        const descriptor = Object.getOwnPropertyDescriptor(current, key);\n        if (descriptor) {\n            if (typeof descriptor.set === 'function') {\n                descriptor.set.call(container, entry);\n                return true;\n            }\n            if (descriptor.writable) {\n                Object.defineProperty(container, key, {\n                    configurable: descriptor.configurable ?? true,\n                    enumerable: descriptor.enumerable ?? true,\n                    value: entry,\n                    writable: true,\n                });\n                return true;\n            }\n            return false;\n        }\n        current = Object.getPrototypeOf(current);\n    }\n    Object.defineProperty(container, key, {\n        configurable: true,\n        enumerable: true,\n        value: entry,\n        writable: true,\n    });\n    return true;\n}\n// php/xdiff/xdiff_string_patch\nfunction xdiff_string_patch(originalStr, patch, flags, errorObj) {\n    //      discuss at: https://locutus.io/php/xdiff_string_patch/\n    // parity verified: PHP 8.3\n    //     original by: Brett Zamir (https://brett-zamir.me)\n    //     improved by: Steven Levithan (stevenlevithan.com)\n    //          note 1: The XDIFF_PATCH_IGNORESPACE flag and the error argument are not\n    //          note 1: currently supported.\n    //          note 2: This has not been tested exhaustively yet.\n    //          note 3: The errorObj parameter (optional) if used must be passed in as a\n    //          note 3: object. The errors will then be written by reference into it's `value` property\n    //       example 1: xdiff_string_patch('', '@@ -0,0 +1,1 @@\\n+Hello world!')\n    //       returns 1: 'Hello world!'\n    // First two functions were adapted from Steven Levithan, also under an MIT license\n    // Adapted from XRegExp 1.5.0\n    // (c) 2007-2010 Steven Levithan\n    // MIT License\n    // <https://xregexp.com>\n    const isRecord = (value) => typeof value === 'object' && value !== null && !Array.isArray(value);\n    const _getNativeFlags = function (regex) {\n        const extended = getPhpObjectEntry(regex, 'extended') === true;\n        const sticky = getPhpObjectEntry(regex, 'sticky') === true;\n        // Proposed for ES4; included in AS3\n        return [\n            regex.global ? 'g' : '',\n            regex.ignoreCase ? 'i' : '',\n            regex.multiline ? 'm' : '',\n            extended ? 'x' : '',\n            sticky ? 'y' : '',\n        ].join('');\n    };\n    const getXRegExpMeta = (regex) => {\n        const xregexpValue = getPhpObjectEntry(regex, '_xregexp');\n        if (!isRecord(xregexpValue)) {\n            return undefined;\n        }\n        const sourceValue = getPhpObjectEntry(xregexpValue, 'source');\n        if (typeof sourceValue !== 'string') {\n            return undefined;\n        }\n        const captureNamesValue = getPhpObjectEntry(xregexpValue, 'captureNames');\n        let captureNames;\n        if (captureNamesValue === null) {\n            captureNames = null;\n        }\n        else if (Array.isArray(captureNamesValue) &&\n            captureNamesValue.every((captureName) => typeof captureName === 'string')) {\n            captureNames = captureNamesValue;\n        }\n        if (captureNames === undefined) {\n            return { source: sourceValue };\n        }\n        return {\n            source: sourceValue,\n            captureNames,\n        };\n    };\n    const _cbSplit = function (input, sep) {\n        // If separator `s` is not a regex, use the native `split`\n        if (!(sep instanceof RegExp)) {\n            return input.split(sep);\n        }\n        const str = String(input);\n        const output = [];\n        let lastLastIndex = 0;\n        let match = null;\n        let lastLength = 0;\n        const limit = Infinity;\n        const x = getXRegExpMeta(sep);\n        // This is required if not `s.global`, and it avoids needing to set `s.lastIndex` to zero\n        // and restore it to its original value when we're done using the regex\n        // Brett paring down\n        const s = new RegExp(sep.source, _getNativeFlags(sep) + 'g');\n        if (x) {\n            setPhpObjectEntry(s, '_xregexp', {\n                source: x.source,\n                captureNames: x.captureNames ? x.captureNames.slice(0) : null,\n            });\n        }\n        while ((match = s.exec(str))) {\n            // Run the altered `exec` (required for `lastIndex` fix, etc.)\n            if (s.lastIndex > lastLastIndex) {\n                output.push(str.slice(lastLastIndex, match.index));\n                if (match.length > 1 && match.index < str.length) {\n                    const captures = match.slice(1).filter((capture) => capture !== undefined);\n                    Array.prototype.push.apply(output, captures);\n                }\n                lastLength = match[0].length;\n                lastLastIndex = s.lastIndex;\n                if (output.length >= limit) {\n                    break;\n                }\n            }\n            if (s.lastIndex === match.index) {\n                s.lastIndex++;\n            }\n        }\n        if (lastLastIndex === str.length) {\n            if (!s.test('') || lastLength) {\n                output.push('');\n            }\n        }\n        else {\n            output.push(str.slice(lastLastIndex));\n        }\n        return output.length > limit ? output.slice(0, limit) : output;\n    };\n    // Input defaulting & sanitation\n    if (!patch) {\n        return false;\n    }\n    let i = 0;\n    let ll = 0;\n    let ranges = null;\n    let lastLinePos = 0;\n    let firstChar = '';\n    const rangeExp = /^@@\\s+-(\\d+),(\\d+)\\s+\\+(\\d+),(\\d+)\\s+@@$/;\n    const lineBreaks = /\\r?\\n/;\n    const lines = _cbSplit(patch.replace(/(\\r?\\n)+$/, ''), lineBreaks);\n    const origLines = _cbSplit(originalStr, lineBreaks);\n    const newStrArr = [];\n    let linePos = 0;\n    const errors = '';\n    let optTemp = 0; // Both string & integer (constant) input is allowed\n    const OPTS = {\n        // Unsure of actual PHP values, so better to rely on string\n        XDIFF_PATCH_NORMAL: 1,\n        XDIFF_PATCH_REVERSE: 2,\n        XDIFF_PATCH_IGNORESPACE: 4,\n    };\n    if (!flags) {\n        flags = 'XDIFF_PATCH_NORMAL';\n    }\n    if (typeof flags !== 'number') {\n        // Allow for a single string or an array of string flags\n        const flagList = Array.isArray(flags) ? flags : [flags];\n        const isPatchOption = (value) => value in OPTS;\n        for (i = 0; i < flagList.length; i++) {\n            const currentFlag = flagList[i];\n            if (!currentFlag) {\n                continue;\n            }\n            // Resolve string input to bitwise e.g. 'XDIFF_PATCH_NORMAL' becomes 1\n            if (isPatchOption(currentFlag)) {\n                optTemp = optTemp | OPTS[currentFlag];\n            }\n        }\n        flags = optTemp;\n    }\n    if (flags & OPTS.XDIFF_PATCH_NORMAL) {\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[1]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[1]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case '+':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    else if (flags & OPTS.XDIFF_PATCH_REVERSE) {\n        // Only differs from above by a few lines\n        for (i = 0, ll = lines.length; i < ll; i++) {\n            const line = lines[i] ?? '';\n            ranges = line.match(rangeExp);\n            if (ranges?.[3]) {\n                lastLinePos = linePos;\n                linePos = Number(ranges[3]) - 1;\n                while (lastLinePos < linePos) {\n                    newStrArr.push(origLines[lastLinePos++] ?? '');\n                }\n                while (lines[++i] !== undefined && rangeExp.exec(lines[i] ?? '') === null) {\n                    const patchedLine = lines[i] ?? '';\n                    firstChar = patchedLine.charAt(0);\n                    switch (firstChar) {\n                        case '-':\n                            newStrArr.push(patchedLine.slice(1));\n                            break;\n                        case '+':\n                            // Skip including that line\n                            ++linePos;\n                            break;\n                        case ' ':\n                            newStrArr.push(origLines[linePos++] ?? '');\n                            break;\n                        default:\n                            // Reconcile with returning errrors arg?\n                            throw new Error('Unrecognized initial character in unidiff line');\n                    }\n                }\n                if (lines[i]) {\n                    i--;\n                }\n            }\n        }\n        while (linePos > 0 && linePos < origLines.length) {\n            newStrArr.push(origLines[linePos++] ?? '');\n        }\n    }\n    if (errorObj) {\n        errorObj.value = errors;\n    }\n    return newStrArr.join('\\n');\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as Record<string, unknown> }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/php/xdiff/xdiff_string_patch.ts (tested in test/generated/php/xdiff/xdiff_string_patch.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = 'Hello world!'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (xdiff_string_patch: typeof __locutus_source_fn) => {
      return xdiff_string_patch('', '@@ -0,0 +1,1 @@\n+Hello world!')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
