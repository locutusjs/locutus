// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const ini_set = require('../../../../src/php/info/ini_set.ts').ini_set
const ini_get = require('../../../../src/php/info/ini_get.ts').ini_get
const __locutus_source_fn = require('../../../../src/php/misc/uniqid.ts').uniqid
const __locutus_source_module_url = new URL("../../../../src/php/misc/uniqid.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "uniqid"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.uniqid = uniqid;\nconst _phpRuntimeState_ts_1 = require(\"../_helpers/_phpRuntimeState.ts\");\nfunction uniqid(prefix, moreEntropy) {\n    //  discuss at: https://locutus.io/php/uniqid/\n    // original by: Kevin van Zonneveld (https://kvz.io)\n    //  revised by: Kankrelune (https://www.webfaktory.info/)\n    //      note 1: Uses an internal counter (in locutus global) to avoid collision\n    //   example 1: var $id = uniqid()\n    //   example 1: var $result = $id.length === 13\n    //   returns 1: true\n    //   example 2: var $id = uniqid('foo')\n    //   example 2: var $result = $id.length === (13 + 'foo'.length)\n    //   returns 2: true\n    //   example 3: var $id = uniqid('bar', true)\n    //   example 3: var $result = $id.length === (23 + 'bar'.length)\n    //   returns 3: true\n    const normalizedPrefix = prefix ?? '';\n    let retId = '';\n    const formatSeed = (seed, reqWidth) => {\n        const hexSeed = Number.parseInt(String(seed), 10).toString(16); // to hex str\n        if (reqWidth < hexSeed.length) {\n            // so long we split\n            return hexSeed.slice(hexSeed.length - reqWidth);\n        }\n        if (reqWidth > hexSeed.length) {\n            // so short we pad\n            return new Array(1 + (reqWidth - hexSeed.length)).join('0') + hexSeed;\n        }\n        return hexSeed;\n    };\n    let uniqidSeed = (0, _phpRuntimeState_ts_1.getPhpRuntimeNumber)('uniqidSeed', Math.floor(Math.random() * 0x75bcd15)); // init seed with big random int\n    uniqidSeed++;\n    (0, _phpRuntimeState_ts_1.setPhpRuntimeEntry)('uniqidSeed', uniqidSeed);\n    // start with prefix, add current milliseconds hex string\n    retId = normalizedPrefix;\n    retId += formatSeed(Number.parseInt(String(new Date().getTime() / 1000), 10), 8);\n    // add seed hex string\n    retId += formatSeed(uniqidSeed, 5);\n    if (moreEntropy) {\n        // for more entropy we add a float lower to 10\n        retId += (Math.random() * 10).toFixed(8).toString();\n    }\n    return retId;\n}"
const __locutus_standalone_ts_code = "const globalContext = typeof window === 'object' && window !== null ? window : typeof global === 'object' && global !== null ? global : {};\nconst ensurePhpRuntimeObject = () => {\n    let locutus = globalContext.$locutus;\n    if (typeof locutus !== 'object' || locutus === null) {\n        locutus = {};\n        globalContext.$locutus = locutus;\n    }\n    let php = locutus.php;\n    if (typeof php !== 'object' || php === null) {\n        php = {};\n        locutus.php = php;\n    }\n    return php;\n};\nfunction getPhpRuntimeEntry(key) {\n    const php = ensurePhpRuntimeObject();\n    const value = php[key];\n    return typeof value === 'undefined' ? undefined : value;\n}\nfunction setPhpRuntimeEntry(key, value) {\n    const php = ensurePhpRuntimeObject();\n    php[key] = value;\n}\nfunction getPhpRuntimeNumber(key, fallback) {\n    const value = getPhpRuntimeEntry(key);\n    return typeof value === 'number' ? value : fallback;\n}\n// php/misc/uniqid (target function module)\nfunction uniqid(prefix, moreEntropy) {\n    //  discuss at: https://locutus.io/php/uniqid/\n    // original by: Kevin van Zonneveld (https://kvz.io)\n    //  revised by: Kankrelune (https://www.webfaktory.info/)\n    //      note 1: Uses an internal counter (in locutus global) to avoid collision\n    //   example 1: var $id = uniqid()\n    //   example 1: var $result = $id.length === 13\n    //   returns 1: true\n    //   example 2: var $id = uniqid('foo')\n    //   example 2: var $result = $id.length === (13 + 'foo'.length)\n    //   returns 2: true\n    //   example 3: var $id = uniqid('bar', true)\n    //   example 3: var $result = $id.length === (23 + 'bar'.length)\n    //   returns 3: true\n    const normalizedPrefix = prefix ?? '';\n    let retId = '';\n    const formatSeed = (seed, reqWidth) => {\n        const hexSeed = Number.parseInt(String(seed), 10).toString(16); // to hex str\n        if (reqWidth < hexSeed.length) {\n            // so long we split\n            return hexSeed.slice(hexSeed.length - reqWidth);\n        }\n        if (reqWidth > hexSeed.length) {\n            // so short we pad\n            return new Array(1 + (reqWidth - hexSeed.length)).join('0') + hexSeed;\n        }\n        return hexSeed;\n    };\n    let uniqidSeed = getPhpRuntimeNumber('uniqidSeed', Math.floor(Math.random() * 0x75bcd15)); // init seed with big random int\n    uniqidSeed++;\n    setPhpRuntimeEntry('uniqidSeed', uniqidSeed);\n    // start with prefix, add current milliseconds hex string\n    retId = normalizedPrefix;\n    retId += formatSeed(Number.parseInt(String(new Date().getTime() / 1000), 10), 8);\n    // add seed hex string\n    retId += formatSeed(uniqidSeed, 5);\n    if (moreEntropy) {\n        // for more entropy we add a float lower to 10\n        retId += (Math.random() * 10).toFixed(8).toString();\n    }\n    return retId;\n}"
const __locutus_standalone_js_code = "// php/_helpers/_phpRuntimeState (Locutus helper dependency)\nconst globalContext = typeof window === 'object' && window !== null ? window : typeof global === 'object' && global !== null ? global : {};\nconst ensurePhpRuntimeObject = () => {\n    let locutus = globalContext.$locutus;\n    if (typeof locutus !== 'object' || locutus === null) {\n        locutus = {};\n        globalContext.$locutus = locutus;\n    }\n    let php = locutus.php;\n    if (typeof php !== 'object' || php === null) {\n        php = {};\n        locutus.php = php;\n    }\n    return php;\n};\nfunction getPhpRuntimeEntry(key) {\n    const php = ensurePhpRuntimeObject();\n    const value = php[key];\n    return typeof value === 'undefined' ? undefined : value;\n}\nfunction setPhpRuntimeEntry(key, value) {\n    const php = ensurePhpRuntimeObject();\n    php[key] = value;\n}\nfunction getPhpRuntimeNumber(key, fallback) {\n    const value = getPhpRuntimeEntry(key);\n    return typeof value === 'number' ? value : fallback;\n}\n// php/misc/uniqid (target function module)\nfunction uniqid(prefix, moreEntropy) {\n    //  discuss at: https://locutus.io/php/uniqid/\n    // original by: Kevin van Zonneveld (https://kvz.io)\n    //  revised by: Kankrelune (https://www.webfaktory.info/)\n    //      note 1: Uses an internal counter (in locutus global) to avoid collision\n    //   example 1: var $id = uniqid()\n    //   example 1: var $result = $id.length === 13\n    //   returns 1: true\n    //   example 2: var $id = uniqid('foo')\n    //   example 2: var $result = $id.length === (13 + 'foo'.length)\n    //   returns 2: true\n    //   example 3: var $id = uniqid('bar', true)\n    //   example 3: var $result = $id.length === (23 + 'bar'.length)\n    //   returns 3: true\n    const normalizedPrefix = prefix ?? '';\n    let retId = '';\n    const formatSeed = (seed, reqWidth) => {\n        const hexSeed = Number.parseInt(String(seed), 10).toString(16); // to hex str\n        if (reqWidth < hexSeed.length) {\n            // so long we split\n            return hexSeed.slice(hexSeed.length - reqWidth);\n        }\n        if (reqWidth > hexSeed.length) {\n            // so short we pad\n            return new Array(1 + (reqWidth - hexSeed.length)).join('0') + hexSeed;\n        }\n        return hexSeed;\n    };\n    let uniqidSeed = getPhpRuntimeNumber('uniqidSeed', Math.floor(Math.random() * 0x75bcd15)); // init seed with big random int\n    uniqidSeed++;\n    setPhpRuntimeEntry('uniqidSeed', uniqidSeed);\n    // start with prefix, add current milliseconds hex string\n    retId = normalizedPrefix;\n    retId += formatSeed(Number.parseInt(String(new Date().getTime() / 1000), 10), 8);\n    // add seed hex string\n    retId += formatSeed(uniqidSeed, 5);\n    if (moreEntropy) {\n        // for more entropy we add a float lower to 10\n        retId += (Math.random() * 10).toFixed(8).toString();\n    }\n    return retId;\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as { [key: string]: unknown } }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/php/misc/uniqid.ts (tested in test/generated/php/misc/uniqid.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = true
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (uniqid: typeof __locutus_source_fn) => {
      var $id = uniqid()
      return $id.length === 13
    }
    for (const __locutus_variant of __locutus_variants) {
      ;(globalThis as { $locutus?: unknown }).$locutus = undefined
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 2', function () {
    const expected = true
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (uniqid: typeof __locutus_source_fn) => {
      var $id = uniqid('foo')
      return $id.length === (13 + 'foo'.length)
    }
    for (const __locutus_variant of __locutus_variants) {
      ;(globalThis as { $locutus?: unknown }).$locutus = undefined
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 3', function () {
    const expected = true
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (uniqid: typeof __locutus_source_fn) => {
      var $id = uniqid('bar', true)
      return $id.length === (23 + 'bar'.length)
    }
    for (const __locutus_variant of __locutus_variants) {
      ;(globalThis as { $locutus?: unknown }).$locutus = undefined
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
