// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const __locutus_source_fn = require('../../../../src/golang/filepath/Rel.ts').Rel
const __locutus_source_module_url = new URL("../../../../src/golang/filepath/Rel.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "Rel"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.Rel = Rel;\nconst cleanPath = (path) => {\n    if (path === '') {\n        return '.';\n    }\n    const rooted = path.startsWith('/');\n    const segments = path.split('/');\n    const stack = [];\n    for (const segment of segments) {\n        if (segment === '' || segment === '.') {\n            continue;\n        }\n        if (segment === '..') {\n            const top = stack.at(-1);\n            if (top && top !== '..') {\n                stack.pop();\n            }\n            else if (!rooted) {\n                stack.push('..');\n            }\n            continue;\n        }\n        stack.push(segment);\n    }\n    const cleaned = `${rooted ? '/' : ''}${stack.join('/')}`;\n    if (cleaned === '') {\n        return rooted ? '/' : '.';\n    }\n    return cleaned;\n};\nconst toSegments = (cleanedPath) => {\n    if (cleanedPath === '.' || cleanedPath === '/') {\n        return [];\n    }\n    return cleanedPath.split('/').filter(Boolean);\n};\nfunction Rel(basePath, targetPath) {\n    //      discuss at: https://locutus.io/golang/filepath/Rel\n    // parity verified: Go 1.23\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Computes a lexical relative path for slash-separated inputs without consulting the current working directory.\n    //          note 2: Throws when one path is rooted and the other is relative, matching Go's error case.\n    //       example 1: Rel('a/b', 'a/b/c')\n    //       returns 1: 'c'\n    //       example 2: Rel('/a/b', '/a/c')\n    //       returns 2: '../c'\n    //       example 3: Rel('a/.', 'a/b/..')\n    //       returns 3: '.'\n    const baseValue = String(basePath);\n    const targetValue = String(targetPath);\n    const cleanBase = cleanPath(baseValue);\n    const cleanTarget = cleanPath(targetValue);\n    const baseRooted = cleanBase.startsWith('/');\n    const targetRooted = cleanTarget.startsWith('/');\n    if (baseRooted !== targetRooted) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const baseSegments = toSegments(cleanBase);\n    const targetSegments = toSegments(cleanTarget);\n    let shared = 0;\n    while (shared < baseSegments.length &&\n        shared < targetSegments.length &&\n        baseSegments[shared] === targetSegments[shared]) {\n        shared += 1;\n    }\n    if (baseSegments.slice(shared).includes('..')) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const relativeSegments = [\n        ...Array.from({ length: baseSegments.length - shared }, () => '..'),\n        ...targetSegments.slice(shared),\n    ];\n    return relativeSegments.length > 0 ? relativeSegments.join('/') : '.';\n}"
const __locutus_standalone_ts_code = "// golang/filepath/Rel (target function module)\nconst cleanPath = (path) => {\n    if (path === '') {\n        return '.';\n    }\n    const rooted = path.startsWith('/');\n    const segments = path.split('/');\n    const stack = [];\n    for (const segment of segments) {\n        if (segment === '' || segment === '.') {\n            continue;\n        }\n        if (segment === '..') {\n            const top = stack.at(-1);\n            if (top && top !== '..') {\n                stack.pop();\n            }\n            else if (!rooted) {\n                stack.push('..');\n            }\n            continue;\n        }\n        stack.push(segment);\n    }\n    const cleaned = `${rooted ? '/' : ''}${stack.join('/')}`;\n    if (cleaned === '') {\n        return rooted ? '/' : '.';\n    }\n    return cleaned;\n};\nconst toSegments = (cleanedPath) => {\n    if (cleanedPath === '.' || cleanedPath === '/') {\n        return [];\n    }\n    return cleanedPath.split('/').filter(Boolean);\n};\nfunction Rel(basePath, targetPath) {\n    //      discuss at: https://locutus.io/golang/filepath/Rel\n    // parity verified: Go 1.23\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Computes a lexical relative path for slash-separated inputs without consulting the current working directory.\n    //          note 2: Throws when one path is rooted and the other is relative, matching Go's error case.\n    //       example 1: Rel('a/b', 'a/b/c')\n    //       returns 1: 'c'\n    //       example 2: Rel('/a/b', '/a/c')\n    //       returns 2: '../c'\n    //       example 3: Rel('a/.', 'a/b/..')\n    //       returns 3: '.'\n    const baseValue = String(basePath);\n    const targetValue = String(targetPath);\n    const cleanBase = cleanPath(baseValue);\n    const cleanTarget = cleanPath(targetValue);\n    const baseRooted = cleanBase.startsWith('/');\n    const targetRooted = cleanTarget.startsWith('/');\n    if (baseRooted !== targetRooted) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const baseSegments = toSegments(cleanBase);\n    const targetSegments = toSegments(cleanTarget);\n    let shared = 0;\n    while (shared < baseSegments.length &&\n        shared < targetSegments.length &&\n        baseSegments[shared] === targetSegments[shared]) {\n        shared += 1;\n    }\n    if (baseSegments.slice(shared).includes('..')) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const relativeSegments = [\n        ...Array.from({ length: baseSegments.length - shared }, () => '..'),\n        ...targetSegments.slice(shared),\n    ];\n    return relativeSegments.length > 0 ? relativeSegments.join('/') : '.';\n}"
const __locutus_standalone_js_code = "// golang/filepath/Rel (target function module)\nconst cleanPath = (path) => {\n    if (path === '') {\n        return '.';\n    }\n    const rooted = path.startsWith('/');\n    const segments = path.split('/');\n    const stack = [];\n    for (const segment of segments) {\n        if (segment === '' || segment === '.') {\n            continue;\n        }\n        if (segment === '..') {\n            const top = stack.at(-1);\n            if (top && top !== '..') {\n                stack.pop();\n            }\n            else if (!rooted) {\n                stack.push('..');\n            }\n            continue;\n        }\n        stack.push(segment);\n    }\n    const cleaned = `${rooted ? '/' : ''}${stack.join('/')}`;\n    if (cleaned === '') {\n        return rooted ? '/' : '.';\n    }\n    return cleaned;\n};\nconst toSegments = (cleanedPath) => {\n    if (cleanedPath === '.' || cleanedPath === '/') {\n        return [];\n    }\n    return cleanedPath.split('/').filter(Boolean);\n};\nfunction Rel(basePath, targetPath) {\n    //      discuss at: https://locutus.io/golang/filepath/Rel\n    // parity verified: Go 1.23\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Computes a lexical relative path for slash-separated inputs without consulting the current working directory.\n    //          note 2: Throws when one path is rooted and the other is relative, matching Go's error case.\n    //       example 1: Rel('a/b', 'a/b/c')\n    //       returns 1: 'c'\n    //       example 2: Rel('/a/b', '/a/c')\n    //       returns 2: '../c'\n    //       example 3: Rel('a/.', 'a/b/..')\n    //       returns 3: '.'\n    const baseValue = String(basePath);\n    const targetValue = String(targetPath);\n    const cleanBase = cleanPath(baseValue);\n    const cleanTarget = cleanPath(targetValue);\n    const baseRooted = cleanBase.startsWith('/');\n    const targetRooted = cleanTarget.startsWith('/');\n    if (baseRooted !== targetRooted) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const baseSegments = toSegments(cleanBase);\n    const targetSegments = toSegments(cleanTarget);\n    let shared = 0;\n    while (shared < baseSegments.length &&\n        shared < targetSegments.length &&\n        baseSegments[shared] === targetSegments[shared]) {\n        shared += 1;\n    }\n    if (baseSegments.slice(shared).includes('..')) {\n        throw new TypeError(`Rel(): can't make ${targetValue} relative to ${baseValue}`);\n    }\n    const relativeSegments = [\n        ...Array.from({ length: baseSegments.length - shared }, () => '..'),\n        ...targetSegments.slice(shared),\n    ];\n    return relativeSegments.length > 0 ? relativeSegments.join('/') : '.';\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as { [key: string]: unknown } }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/golang/filepath/Rel.ts (tested in test/generated/golang/filepath/Rel.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = 'c'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (Rel: typeof __locutus_source_fn) => {
      return Rel('a/b', 'a/b/c')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 2', function () {
    const expected = '../c'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (Rel: typeof __locutus_source_fn) => {
      return Rel('/a/b', '/a/c')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 3', function () {
    const expected = '.'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (Rel: typeof __locutus_source_fn) => {
      return Rel('a/.', 'a/b/..')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
