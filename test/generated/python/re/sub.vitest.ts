// warning: This file is auto generated by `yarn build:tests`
// Do not edit by hand!

import { createRequire } from 'node:module'
import { describe, it, expect } from 'vitest'

process.env.TZ = 'UTC'
const __locutus_source_fn = require('../../../../src/python/re/sub.ts').sub
const __locutus_source_module_url = new URL("../../../../src/python/re/sub.ts", import.meta.url)
const __locutus_source_require = createRequire(__locutus_source_module_url)
const __locutus_func_name = "sub"
const __locutus_module_js_code = "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.sub = sub;\nconst PY_RE_IGNORECASE = 2;\nconst PY_RE_MULTILINE = 8;\nconst PY_RE_DOTALL = 16;\nfunction sub(pattern, replacement, source, count = 0, flags = 0) {\n    //      discuss at: https://locutus.io/python/re/sub/\n    // parity verified: Python 3.12\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Replaces regex matches in a string, similar to Python's re.sub.\n    //          note 2: Supports count-limited substitution and a subset of numeric flags (IGNORECASE=2, MULTILINE=8, DOTALL=16).\n    //       example 1: sub('a+', '-', 'caaab')\n    //       returns 1: 'c-b'\n    //       example 2: sub('(\\\\d+)', '#', 'a1b22c333')\n    //       returns 2: 'a#b#c#'\n    //       example 3: sub('x', 'y', 'xxx', 2)\n    //       returns 3: 'yyx'\n    //       example 4: sub('abc', 'X', 'ABC abc', 0, 2)\n    //       returns 4: 'X X'\n    const input = String(source);\n    const maxCount = normalizeCount(count);\n    const regex = createGlobalRegex(pattern, flags);\n    const template = typeof replacement === 'string' ? replacement : null;\n    let out = '';\n    let lastIndex = 0;\n    let replaced = 0;\n    regex.lastIndex = 0;\n    while (maxCount === 0 || replaced < maxCount) {\n        const match = regex.exec(input);\n        if (!match) {\n            break;\n        }\n        const full = match[0] ?? '';\n        const start = match.index;\n        const end = start + full.length;\n        out += input.slice(lastIndex, start);\n        out +=\n            template === null\n                ? String(replacement(full, ...match.slice(1)))\n                : applyPythonReplacementTemplate(template, full, match.slice(1), match.groups);\n        lastIndex = end;\n        replaced += 1;\n        if (full === '') {\n            if (regex.lastIndex >= input.length) {\n                break;\n            }\n            regex.lastIndex += 1;\n        }\n    }\n    out += input.slice(lastIndex);\n    return replaced > 0 ? out : input;\n}\nfunction normalizeCount(count) {\n    const n = Number(count);\n    if (!Number.isFinite(n) || n <= 0) {\n        return 0;\n    }\n    return Math.floor(n);\n}\nfunction createGlobalRegex(pattern, flags) {\n    const source = pattern instanceof RegExp ? pattern.source : String(pattern);\n    const base = pattern instanceof RegExp ? pattern.flags : '';\n    const extra = normalizeRegexFlags(flags);\n    const combined = dedupeFlags((base + extra).replace(/g/g, '').replace(/y/g, ''));\n    return new RegExp(source, `${combined}g`);\n}\nfunction normalizeRegexFlags(flags) {\n    if (typeof flags === 'string') {\n        return dedupeFlags(flags.replace(/[^dgimsuvy]/g, ''));\n    }\n    let out = '';\n    if (flags & PY_RE_IGNORECASE) {\n        out += 'i';\n    }\n    if (flags & PY_RE_MULTILINE) {\n        out += 'm';\n    }\n    if (flags & PY_RE_DOTALL) {\n        out += 's';\n    }\n    return out;\n}\nfunction dedupeFlags(flags) {\n    const seen = new Set();\n    let out = '';\n    for (const flag of flags) {\n        if (!seen.has(flag)) {\n            seen.add(flag);\n            out += flag;\n        }\n    }\n    return out;\n}\nfunction applyPythonReplacementTemplate(template, full, groups, named) {\n    let out = '';\n    for (let i = 0; i < template.length; i++) {\n        const char = template[i];\n        if (char !== '\\\\') {\n            out += char;\n            continue;\n        }\n        const next = template[i + 1];\n        if (!next) {\n            out += '\\\\';\n            continue;\n        }\n        if (/\\d/.test(next)) {\n            let token = next;\n            let j = i + 2;\n            while (j < template.length && /\\d/.test(template[j] ?? '') && token.length < 2) {\n                token += template[j];\n                j += 1;\n            }\n            out += groups[Number.parseInt(token, 10) - 1] ?? '';\n            i = j - 1;\n            continue;\n        }\n        if (next === 'g' && template[i + 2] === '<') {\n            const close = template.indexOf('>', i + 3);\n            if (close > -1) {\n                const token = template.slice(i + 3, close);\n                if (/^\\d+$/.test(token)) {\n                    out += groups[Number.parseInt(token, 10) - 1] ?? '';\n                }\n                else {\n                    out += named?.[token] ?? '';\n                }\n                i = close;\n                continue;\n            }\n        }\n        const simpleEscapes = {\n            '\\\\': '\\\\',\n            n: '\\n',\n            r: '\\r',\n            t: '\\t',\n            f: '\\f',\n            v: '\\v',\n            a: '\\x07',\n            b: '\\b',\n        };\n        if (simpleEscapes[next] !== undefined) {\n            out += simpleEscapes[next];\n            i += 1;\n            continue;\n        }\n        if (next === '0') {\n            out += full;\n            i += 1;\n            continue;\n        }\n        out += next;\n        i += 1;\n    }\n    return out;\n}"
const __locutus_standalone_ts_code = "const PY_RE_IGNORECASE = 2;\nconst PY_RE_MULTILINE = 8;\nconst PY_RE_DOTALL = 16;\nfunction sub(pattern, replacement, source, count = 0, flags = 0) {\n    //      discuss at: https://locutus.io/python/re/sub/\n    // parity verified: Python 3.12\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Replaces regex matches in a string, similar to Python's re.sub.\n    //          note 2: Supports count-limited substitution and a subset of numeric flags (IGNORECASE=2, MULTILINE=8, DOTALL=16).\n    //       example 1: sub('a+', '-', 'caaab')\n    //       returns 1: 'c-b'\n    //       example 2: sub('(\\\\d+)', '#', 'a1b22c333')\n    //       returns 2: 'a#b#c#'\n    //       example 3: sub('x', 'y', 'xxx', 2)\n    //       returns 3: 'yyx'\n    //       example 4: sub('abc', 'X', 'ABC abc', 0, 2)\n    //       returns 4: 'X X'\n    const input = String(source);\n    const maxCount = normalizeCount(count);\n    const regex = createGlobalRegex(pattern, flags);\n    const template = typeof replacement === 'string' ? replacement : null;\n    let out = '';\n    let lastIndex = 0;\n    let replaced = 0;\n    regex.lastIndex = 0;\n    while (maxCount === 0 || replaced < maxCount) {\n        const match = regex.exec(input);\n        if (!match) {\n            break;\n        }\n        const full = match[0] ?? '';\n        const start = match.index;\n        const end = start + full.length;\n        out += input.slice(lastIndex, start);\n        out +=\n            template === null\n                ? String(replacement(full, ...match.slice(1)))\n                : applyPythonReplacementTemplate(template, full, match.slice(1), match.groups);\n        lastIndex = end;\n        replaced += 1;\n        if (full === '') {\n            if (regex.lastIndex >= input.length) {\n                break;\n            }\n            regex.lastIndex += 1;\n        }\n    }\n    out += input.slice(lastIndex);\n    return replaced > 0 ? out : input;\n}\nfunction normalizeCount(count) {\n    const n = Number(count);\n    if (!Number.isFinite(n) || n <= 0) {\n        return 0;\n    }\n    return Math.floor(n);\n}\nfunction createGlobalRegex(pattern, flags) {\n    const source = pattern instanceof RegExp ? pattern.source : String(pattern);\n    const base = pattern instanceof RegExp ? pattern.flags : '';\n    const extra = normalizeRegexFlags(flags);\n    const combined = dedupeFlags((base + extra).replace(/g/g, '').replace(/y/g, ''));\n    return new RegExp(source, `${combined}g`);\n}\nfunction normalizeRegexFlags(flags) {\n    if (typeof flags === 'string') {\n        return dedupeFlags(flags.replace(/[^dgimsuvy]/g, ''));\n    }\n    let out = '';\n    if (flags & PY_RE_IGNORECASE) {\n        out += 'i';\n    }\n    if (flags & PY_RE_MULTILINE) {\n        out += 'm';\n    }\n    if (flags & PY_RE_DOTALL) {\n        out += 's';\n    }\n    return out;\n}\nfunction dedupeFlags(flags) {\n    const seen = new Set();\n    let out = '';\n    for (const flag of flags) {\n        if (!seen.has(flag)) {\n            seen.add(flag);\n            out += flag;\n        }\n    }\n    return out;\n}\nfunction applyPythonReplacementTemplate(template, full, groups, named) {\n    let out = '';\n    for (let i = 0; i < template.length; i++) {\n        const char = template[i];\n        if (char !== '\\\\') {\n            out += char;\n            continue;\n        }\n        const next = template[i + 1];\n        if (!next) {\n            out += '\\\\';\n            continue;\n        }\n        if (/\\d/.test(next)) {\n            let token = next;\n            let j = i + 2;\n            while (j < template.length && /\\d/.test(template[j] ?? '') && token.length < 2) {\n                token += template[j];\n                j += 1;\n            }\n            out += groups[Number.parseInt(token, 10) - 1] ?? '';\n            i = j - 1;\n            continue;\n        }\n        if (next === 'g' && template[i + 2] === '<') {\n            const close = template.indexOf('>', i + 3);\n            if (close > -1) {\n                const token = template.slice(i + 3, close);\n                if (/^\\d+$/.test(token)) {\n                    out += groups[Number.parseInt(token, 10) - 1] ?? '';\n                }\n                else {\n                    out += named?.[token] ?? '';\n                }\n                i = close;\n                continue;\n            }\n        }\n        const simpleEscapes = {\n            '\\\\': '\\\\',\n            n: '\\n',\n            r: '\\r',\n            t: '\\t',\n            f: '\\f',\n            v: '\\v',\n            a: '\\x07',\n            b: '\\b',\n        };\n        if (simpleEscapes[next] !== undefined) {\n            out += simpleEscapes[next];\n            i += 1;\n            continue;\n        }\n        if (next === '0') {\n            out += full;\n            i += 1;\n            continue;\n        }\n        out += next;\n        i += 1;\n    }\n    return out;\n}"
const __locutus_standalone_js_code = "// python/re/sub (target function module)\nconst PY_RE_IGNORECASE = 2;\nconst PY_RE_MULTILINE = 8;\nconst PY_RE_DOTALL = 16;\nfunction sub(pattern, replacement, source, count = 0, flags = 0) {\n    //      discuss at: https://locutus.io/python/re/sub/\n    // parity verified: Python 3.12\n    //     original by: Kevin van Zonneveld (https://kvz.io)\n    //          note 1: Replaces regex matches in a string, similar to Python's re.sub.\n    //          note 2: Supports count-limited substitution and a subset of numeric flags (IGNORECASE=2, MULTILINE=8, DOTALL=16).\n    //       example 1: sub('a+', '-', 'caaab')\n    //       returns 1: 'c-b'\n    //       example 2: sub('(\\\\d+)', '#', 'a1b22c333')\n    //       returns 2: 'a#b#c#'\n    //       example 3: sub('x', 'y', 'xxx', 2)\n    //       returns 3: 'yyx'\n    //       example 4: sub('abc', 'X', 'ABC abc', 0, 2)\n    //       returns 4: 'X X'\n    const input = String(source);\n    const maxCount = normalizeCount(count);\n    const regex = createGlobalRegex(pattern, flags);\n    const template = typeof replacement === 'string' ? replacement : null;\n    let out = '';\n    let lastIndex = 0;\n    let replaced = 0;\n    regex.lastIndex = 0;\n    while (maxCount === 0 || replaced < maxCount) {\n        const match = regex.exec(input);\n        if (!match) {\n            break;\n        }\n        const full = match[0] ?? '';\n        const start = match.index;\n        const end = start + full.length;\n        out += input.slice(lastIndex, start);\n        out +=\n            template === null\n                ? String(replacement(full, ...match.slice(1)))\n                : applyPythonReplacementTemplate(template, full, match.slice(1), match.groups);\n        lastIndex = end;\n        replaced += 1;\n        if (full === '') {\n            if (regex.lastIndex >= input.length) {\n                break;\n            }\n            regex.lastIndex += 1;\n        }\n    }\n    out += input.slice(lastIndex);\n    return replaced > 0 ? out : input;\n}\nfunction normalizeCount(count) {\n    const n = Number(count);\n    if (!Number.isFinite(n) || n <= 0) {\n        return 0;\n    }\n    return Math.floor(n);\n}\nfunction createGlobalRegex(pattern, flags) {\n    const source = pattern instanceof RegExp ? pattern.source : String(pattern);\n    const base = pattern instanceof RegExp ? pattern.flags : '';\n    const extra = normalizeRegexFlags(flags);\n    const combined = dedupeFlags((base + extra).replace(/g/g, '').replace(/y/g, ''));\n    return new RegExp(source, `${combined}g`);\n}\nfunction normalizeRegexFlags(flags) {\n    if (typeof flags === 'string') {\n        return dedupeFlags(flags.replace(/[^dgimsuvy]/g, ''));\n    }\n    let out = '';\n    if (flags & PY_RE_IGNORECASE) {\n        out += 'i';\n    }\n    if (flags & PY_RE_MULTILINE) {\n        out += 'm';\n    }\n    if (flags & PY_RE_DOTALL) {\n        out += 's';\n    }\n    return out;\n}\nfunction dedupeFlags(flags) {\n    const seen = new Set();\n    let out = '';\n    for (const flag of flags) {\n        if (!seen.has(flag)) {\n            seen.add(flag);\n            out += flag;\n        }\n    }\n    return out;\n}\nfunction applyPythonReplacementTemplate(template, full, groups, named) {\n    let out = '';\n    for (let i = 0; i < template.length; i++) {\n        const char = template[i];\n        if (char !== '\\\\') {\n            out += char;\n            continue;\n        }\n        const next = template[i + 1];\n        if (!next) {\n            out += '\\\\';\n            continue;\n        }\n        if (/\\d/.test(next)) {\n            let token = next;\n            let j = i + 2;\n            while (j < template.length && /\\d/.test(template[j] ?? '') && token.length < 2) {\n                token += template[j];\n                j += 1;\n            }\n            out += groups[Number.parseInt(token, 10) - 1] ?? '';\n            i = j - 1;\n            continue;\n        }\n        if (next === 'g' && template[i + 2] === '<') {\n            const close = template.indexOf('>', i + 3);\n            if (close > -1) {\n                const token = template.slice(i + 3, close);\n                if (/^\\d+$/.test(token)) {\n                    out += groups[Number.parseInt(token, 10) - 1] ?? '';\n                }\n                else {\n                    out += named?.[token] ?? '';\n                }\n                i = close;\n                continue;\n            }\n        }\n        const simpleEscapes = {\n            '\\\\': '\\\\',\n            n: '\\n',\n            r: '\\r',\n            t: '\\t',\n            f: '\\f',\n            v: '\\v',\n            a: '\\x07',\n            b: '\\b',\n        };\n        if (simpleEscapes[next] !== undefined) {\n            out += simpleEscapes[next];\n            i += 1;\n            continue;\n        }\n        if (next === '0') {\n            out += full;\n            i += 1;\n            continue;\n        }\n        out += next;\n        i += 1;\n    }\n    return out;\n}"

const __locutus_eval_function = (compiledCode: string): ((...args: unknown[]) => unknown) => {
  const evaluator = new Function('require', compiledCode + '\nreturn ' + __locutus_func_name + ';')
  return evaluator(__locutus_source_require) as (...args: unknown[]) => unknown
}
const __locutus_eval_module_export = (compiledCode: string, exportName: string): ((...args: unknown[]) => unknown) => {
  const module = { exports: {} as { [key: string]: unknown } }
  const exports = module.exports
  const evaluator = new Function('exports', 'module', 'require', compiledCode)
  evaluator(exports, module, __locutus_source_require)
  return module.exports[exportName] as (...args: unknown[]) => unknown
}
const __locutus_module_js_fn = __locutus_eval_module_export(__locutus_module_js_code, __locutus_func_name)
const __locutus_standalone_ts_fn = __locutus_eval_function(__locutus_standalone_ts_code)
const __locutus_standalone_js_fn = __locutus_eval_function(__locutus_standalone_js_code)

describe('src/python/re/sub.ts (tested in test/generated/python/re/sub.vitest.ts)', function () {
  it('should pass example 1', function () {
    const expected = 'c-b'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sub: typeof __locutus_source_fn) => {
      return sub('a+', '-', 'caaab')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 2', function () {
    const expected = 'a#b#c#'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sub: typeof __locutus_source_fn) => {
      return sub('(\\d+)', '#', 'a1b22c333')
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 3', function () {
    const expected = 'yyx'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sub: typeof __locutus_source_fn) => {
      return sub('x', 'y', 'xxx', 2)
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
  it('should pass example 4', function () {
    const expected = 'X X'
    const __locutus_variants: Array<{ name: string; fn: (...args: unknown[]) => unknown }> = [
      { name: "source", fn: __locutus_source_fn },
      { name: "module-js", fn: __locutus_module_js_fn },
      { name: "standalone-ts", fn: __locutus_standalone_ts_fn },
      { name: "standalone-js", fn: __locutus_standalone_js_fn },
    ]
    const __locutus_run_example = (sub: typeof __locutus_source_fn) => {
      return sub('abc', 'X', 'ABC abc', 0, 2)
    }
    for (const __locutus_variant of __locutus_variants) {
      const result = __locutus_run_example(__locutus_variant.fn as typeof __locutus_source_fn)
      expect(result).toEqual(expected)
    }
  })
})
